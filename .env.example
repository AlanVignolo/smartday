# ============================================================
# .env.example
# Plantilla de variables de entorno. Copiá este archivo como
# ".env" y completá los valores reales. El archivo .env NUNCA
# se sube a git (contiene claves secretas).
# ============================================================

# --- Entorno general ---
ENV=development          # "development" en local, "production" en el servidor real
DEBUG=true               # Muestra errores detallados. Siempre "false" en producción
SECRET_KEY=tu-secret-key-cambiar-en-produccion  # Clave interna de la app, generala aleatoria

# --- API ---
API_V1_PREFIX=/api/v1   # Prefijo de todas las rutas (ej: /api/v1/tasks)
PROJECT_NAME=SmartDay

# --- Base de datos (PostgreSQL) ---
# Formato: postgresql+asyncpg://usuario:contraseña@host:puerto/nombre_bd
DATABASE_URL=postgresql+asyncpg://smartday:smartday@localhost:5432/smartday

# --- Redis (caché y colas) ---
REDIS_URL=redis://localhost:6379/0          # Base de datos Redis 0 (caché general)
CELERY_BROKER_URL=redis://localhost:6379/1  # Redis 1 para envío de tareas a Celery
CELERY_RESULT_BACKEND=redis://localhost:6379/2  # Redis 2 para guardar resultados

# --- JWT (autenticación) ---
JWT_SECRET_KEY=jwt-secret-key-cambiar      # Clave para firmar los tokens, generala aleatoria
JWT_ALGORITHM=HS256                        # Algoritmo de firma del token
JWT_EXPIRATION_MINUTES=30                  # Tiempo de vida del token de acceso
JWT_REFRESH_EXPIRATION_DAYS=7              # Tiempo de vida del token de renovación

# --- IA ---
# Opcional: solo necesario si usás sugerencias con ChatGPT/OpenAI
OPENAI_API_KEY=sk-xxx

# --- Notificaciones push (Firebase) ---
FIREBASE_CREDENTIALS_PATH=./firebase-credentials.json  # Ruta al archivo de credenciales de Firebase

# --- Logs ---
LOG_LEVEL=INFO  # Qué tan detallado es el registro: DEBUG, INFO, WARNING, ERROR
